<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^(.*) public/$1 [L]
</IfModule>

"Once you've finished the entire project:

1. Zip all the files

2. Upload the ZIP to cPanel

3. Add this .htaccess file to the root directory

That's all - no additional modifications are required."
